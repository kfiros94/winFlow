{
  "installCommand": "npm --prefix winflow-client install",
  "buildCommand": "npm --prefix winflow-client run build",
  "outputDirectory": "winflow-client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
